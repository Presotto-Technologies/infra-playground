testkube-api:
  slackToken: 
  slackTemplate: 
  slackConfig: 

  minio:
    enabled: false

  storage:
    endpoint: ${endpoint}
    accessKeyId: ${accessKeyId}
    accessKey: ${accessKey}

  service:
    annotations: 
      linkerd.io/inject: "enabled"

  prometheus:
    enabled: false

telemetryEnabled: ${telemetry}
